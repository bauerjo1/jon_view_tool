require "spec_helper"

describe JonViewTool do
  it "has a version number" do
    expect(JonViewTool::VERSION).not_to be nil
  end
  
  it "does something useful" do
    expect(false).to eq(true)
  end
end